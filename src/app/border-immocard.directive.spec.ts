import { BorderImmocardDirective } from './border-immocard.directive';

describe('BorderImmocardDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderImmocardDirective();
    expect(directive).toBeTruthy();
  });
});
