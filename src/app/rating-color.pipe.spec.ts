import { RatingColorPipe } from './rating-color.pipe';

describe('RatingColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingColorPipe();
    expect(pipe).toBeTruthy();
  });
});
